name = "mahalyerp"
compatibility_date = "2024-01-01"
pages_build_output_dir = "dist"

# Cloudflare Pages — proxy /api/* إلى Fly.io
# يمكن أيضاً تعيين MAHALY_API_URL من Dashboard → Settings → Variables

[vars]
VITE_API_URL = "/api/v1"
MAHALY_API_URL = "https://mahalyerp-api.fly.dev/api"
