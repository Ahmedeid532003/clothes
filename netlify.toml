[build]
  command = "npm run build:netlify"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"
  # عيّن في Netlify Dashboard → Environment variables (Build):
  # VITE_API_URL = https://YOUR-APP.koyeb.app/api/v1

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
